# Claude Status — macOS Menu Bar App

A native macOS menu bar app that monitors Claude Code usage. Reuses the
existing Claude Code login (Keychain), reads local session logs for token
counts, optionally calls the Anthropic OAuth usage endpoint for
plan-aware data, and shows API-equivalent cost in USD.

- **Bundle id:** `com.bcollard.claudestatus`
- **Display name:** Claude Status
- **Target:** macOS 14+
- **Distribution:** unsigned `.app` via Homebrew cask (Developer ID + notarization is a planned follow-up)

---

## Quick start

```bash
./build.sh                                    # builds .build/Claude Status.app
cp -R ".build/Claude Status.app" /Applications/
open "/Applications/Claude Status.app"
```

The first time you click the menu bar icon, macOS prompts to allow access
to the Keychain entry `Claude Code-credentials` — choose **Always Allow**.

The icon is a ✨ in the menu bar near the right side. On a MacBook Pro with
a notch, the menu bar fills up and items can hide behind the notch. To
find ours: hold ⌘ and drag menu bar icons, or temporarily quit one of your
other menu bar apps.

---

## Architecture

```
claude-status-macos-menu-bar/
├── Package.swift                       # SwiftPM, macOS 14, executable target
├── Resources/
│   ├── Info.plist                      # LSUIElement=true, CFBundleIconFile=AppIcon
│   └── AppIcon.icns                    # generated by scripts/make_icon.swift
├── Sources/ClaudeStatus/
│   ├── App.swift                       # @main, MenuBarExtra entry point
│   ├── UsageStore.swift                # @MainActor ObservableObject; orchestrates refresh
│   ├── KeychainReader.swift            # reads "Claude Code-credentials" — picks freshest entry
│   ├── ClaudeConfigReader.swift        # reads ~/.claude.json oauthAccount (email/org)
│   ├── SessionLogScanner.swift         # streams ~/.claude/projects/**/*.jsonl
│   ├── UsageAPIClient.swift            # GET /api/oauth/usage → list of plan rows
│   ├── Pricing.swift                   # per-million-token price table + cost calc
│   ├── LaunchAtLogin.swift             # SMAppService wrapper
│   └── MenuView.swift                  # dropdown UI
├── Casks/claude-status.rb              # Homebrew cask formula
├── scripts/
│   ├── make_icon.swift                 # generates Resources/AppIcon.icns
│   └── release.sh                      # builds, zips, prints sha256
├── build.sh                            # builds .app bundle + ad-hoc signs
└── .gitignore
```

### Data flow

```
┌────────────────────────┐    ┌───────────────────────────┐
│ ~/.claude/projects/    │    │ macOS Keychain            │
│   **/*.jsonl (token    │    │   "Claude Code-           │
│   usage per turn)      │    │    credentials"           │
└────────┬───────────────┘    └────────────┬──────────────┘
         │                                 │
         ▼                                 ▼
   SessionLogScanner                 KeychainReader
         │                                 │
         └─────────────┬───────────────────┤
                       ▼                   ▼
                  UsageStore        UsageAPIClient ──► api.anthropic.com
                       │                   │             /api/oauth/usage
                       ▼                   │             (Bearer OAuth)
                   MenuView ◄──────────────┘
```

### Refresh model

`UsageStore.refresh()` runs at app start and every 60s (timer). Each
refresh:

1. Scans JSONL files in two date windows (start-of-day, start-of-week) on
   a detached utility-priority task.
2. Re-reads the Keychain entry (cheap; in-memory after first auth).
3. Calls `UsageAPIClient.fetch()` — fails silently if token is expired or
   the endpoint returns a non-2xx.

Manual refresh button in the dropdown calls the same `refresh()` method.

---

## Data sources

### Local session logs

Path: `~/.claude/projects/<sanitized-cwd>/<session-uuid>.jsonl`

Each assistant turn is logged with a `usage` block. Two shapes observed —
top-level or nested under `message`:

```json
{ "timestamp":"2026-05-22T07:53:00.000Z",
  "sessionId":"a30e...",
  "model":"claude-opus-4-7",
  "usage":{
    "input_tokens": 3,
    "output_tokens": 412,
    "cache_creation_input_tokens": 6316,
    "cache_read_input_tokens": 11164
  }}
```

`SessionLogScanner.scan(since:)` streams files line-by-line (64 KB chunks),
filters by `timestamp >= since` (falls back to file mtime), drops
`model == "<synthetic>"`, and aggregates per-model totals.

### Keychain entry — `Claude Code-credentials`

The OAuth blob stored by Claude Code:

```json
{
  "claudeAiOauth": {
    "accessToken":     "sk-ant-oat01-...",
    "refreshToken":    "sk-ant-ort01-...",
    "expiresAt":       1779464425571,   // ms since epoch
    "scopes":          ["user:file_upload","user:inference","user:mcp_servers","user:profile","user:sessions:claude_code"],
    "subscriptionType":"enterprise",     // "pro" | "max" | "team" | "enterprise" | "api"
    "rateLimitTier":   "default_claude_zero"
  }
}
```

`KeychainReader` extracts all of the above. Plan name and rate-limit tier
come **straight from this blob — no API call required**.

**Multiple entries — pick the freshest.** Claude Code can leave several
generic-password items under `svce="Claude Code-credentials"` distinguished
by `acct`:
- `acct=<username>` (older logins, e.g. `jdoe`)
- `acct=claude-code-user` (transitional bootstrap entry)
- `acct=<email>` (current SSO logins, e.g. `you@example.com`)

`KeychainReader.read()` uses a **two-step query** because macOS returns
`errSecParam (-50)` when `kSecMatchLimitAll` is combined with
`kSecReturnData: true`:
1. List all matching entries with attributes only (`kSecReturnAttributes`,
   no data) — sorted by `kSecAttrModificationDate` descending.
2. For each, freshest first, fetch the data via a single-account query
   (`kSecAttrAccount` + `kSecMatchLimitOne` + `kSecReturnData`). Return
   the first non-expired entry; fall back to the freshest parseable entry
   if none are valid.

Without the multi-entry handling we'd lock onto an old, expired entry
forever — the symptom is "token expired" in the dropdown even right after
a fresh `claude /logout` + login.

`SecItemCopyMatching` triggers a one-time per-app authorization prompt;
the user clicks "Always Allow" once.

### Account identity — `~/.claude.json`

Claude Code persists the OAuth account profile in `~/.claude.json` under the
`oauthAccount` key:

```json
{
  "oauthAccount": {
    "accountUuid":       "f0eea998-...",
    "emailAddress":      "you@example.com",
    "displayName":       "Baptiste",
    "organizationUuid":  "b4af20ff-...",
    "organizationName":  "Acme Software",
    "organizationRole":  "user",
    "billingType":       "stripe_subscription_contracted",
    "hasExtraUsageEnabled": true
  }
}
```

`ClaudeConfigReader.read()` returns this — it gives us a real email + org name
for the dropdown header (much nicer than the Keychain `acct` string, which is
just a username).

### Anthropic OAuth usage endpoint (best-effort)

`GET https://api.anthropic.com/api/oauth/usage`

Required headers:
- `Authorization: Bearer <accessToken>`
- `anthropic-beta: oauth-2025-04-20`
- `anthropic-version: 2023-06-01`

Other endpoints discovered in the Claude Code binary
(`/opt/homebrew/Caskroom/claude-code/<version>/claude`):

```
/api/oauth/usage
/api/oauth/profile
/api/oauth/account/settings
/api/oauth/organizations/
/api/oauth/claude_cli/create_api_key
/api/oauth/claude_cli/roles
/api/oauth/file_upload
/api/oauth/files/
```

Refresh endpoint: `https://platform.claude.com/v1/oauth/token`. We do
**not** implement OAuth refresh ourselves (see Known Limitations).

**Response schema** (observed 2026-05 on Enterprise):

```json
{
  "five_hour":             null,
  "seven_day":             null,
  "seven_day_opus":        null,
  "seven_day_sonnet":      null,
  "seven_day_oauth_apps":  null,
  "seven_day_cowork":      null,
  "seven_day_omelette":    null,
  "tangelo":               null,
  "iguana_necktie":        null,
  "omelette_promotional":  { "utilization": 0.0, "resets_at": null },
  "extra_usage": {
    "is_enabled":     true,
    "monthly_limit":  25000,
    "used_credits":   16000.0,
    "utilization":    64.0,
    "currency":       "USD",
    "disabled_reason": null
  }
}
```

- Buckets at the top level are usage pools; `null` means doesn't apply to the
  current plan. For Enterprise most are null; `extra_usage` carries the real
  signal (monthly overage credits, in USD).
- **`used_credits` and `monthly_limit` are minor units (cents), not dollars.**
  Divide by 100 before display — Anthropic follows the standard financial
  convention so the integer storage avoids float drift. E.g. `25000` means
  `$250.00`, not `$25,000`. (Confirmed by cross-checking the figure shown
  at https://claude.ai/settings/usage.)
- `utilization` is already a percentage (0-100), not a fraction.
- For Pro/Max users, `five_hour` and `seven_day` (and per-model variants) are
  expected to be non-null with `{ utilization, resets_at }`.
- `tangelo`, `iguana_necktie`, `omelette_promotional` look like Anthropic
  internal codenames for various pools — we don't render them unless populated.

`UsageAPIClient.summarize()` walks a known list of bucket keys, plus the
`extra_usage` block, and returns an ordered list of `UsageAPIRow(label, value,
detail)` that `MenuView` renders. Unknown shapes degrade to an empty plan
section — local-log data still shows.

---

## Known limitations

### 1. Token refresh

Claude Code's binary refreshes tokens **in-memory** during a session and
does not always write fresh tokens back to the Keychain. To force a fresh
Keychain entry, run `claude /logout` then re-login.

We do **not** implement OAuth refresh ourselves: Anthropic's
`platform.claude.com/v1/oauth/token` returns HTTP 429 for unknown clients
(anti-enumeration) and we don't have a stable client_id from the bundle.

`KeychainReader` picks the entry with the highest `expiresAt`, so the
common "I just logged in but the app still says token expired" trap is
avoided — we won't lock onto a stale `acct=<old-username>` item when a
fresh `acct=<email>` one exists.

If all matching entries are expired, `UsageAPIClient` throws
`tokenExpired` and the dropdown shows
`Keychain token expired N days ago. Open Claude Code to refresh.`

### 2. Notch overflow on MacBook Pro

On notched displays the menu bar overflows behind the notch. The icon is
there but invisible. Workarounds: ⌘-drag menu bar items to reorder, quit
one other menu bar app, or toggle off **Show token count in menu bar** in
the dropdown to reclaim space.

### 3. Pricing is a maintenance hot spot

`Pricing.swift` hardcodes per-million-token rates for Opus / Sonnet /
Haiku. Update when Anthropic publishes new pricing. On plan-based accounts
the cost figure is **API-equivalent**, not what you pay.

### 4. Unsigned, not notarized

Distribution via cask is fine but Gatekeeper will warn on first launch
("downloaded from the internet"). Users right-click → Open the first time.
Notarization requires an Apple Developer ID ($99/year) and adds ~30 lines
to `build.sh` using `xcrun notarytool`.

### 5. App Store distribution would require sandboxing

Reading another app's Keychain entry from inside the sandbox needs a
shared keychain access group entitlement that we can't get without
Anthropic's cooperation. Stick with the cask path.

### 6. Enterprise plan specifics

The Keychain blob reports `rateLimitTier: default_claude_zero` for our
account — Enterprise pools are admin-managed and there's no per-user
weekly cap to display. The dropdown shows plan name + rate-limit tier but
the API rows will likely stay empty for Enterprise.

---

## Build, install, release

### Build

```bash
./build.sh                    # release config, ad-hoc codesign, .build/Claude Status.app
```

Optional: regenerate the app icon after changing the design.
```bash
swift scripts/make_icon.swift  # writes Resources/AppIcon.icns
```

### Local install

```bash
cp -R ".build/Claude Status.app" /Applications/
open "/Applications/Claude Status.app"
```

The app must live in `/Applications` (or `~/Applications`) for
`SMAppService.mainApp.register()` (launch-at-login) to work reliably.

### Release zip (for cask)

```bash
./scripts/release.sh 0.1.0
# → .build/release/ClaudeStatus-0.1.0.zip
# → prints sha256 to paste into Casks/claude-status.rb
```

Uses `ditto -c -k --sequesterRsrc --keepParent` so codesignatures survive
(plain `zip` corrupts signed bundles).

---

## Homebrew cask — publishing checklist

1. **Push this repo to GitHub.**

   ```bash
   git init && git add -A && git commit -m "Initial commit"
   gh repo create claude-status-macos-menu-bar --public --source=. --push
   ```

2. **Cut a release with the zip.**

   ```bash
   ./scripts/release.sh 0.1.0
   gh release create v0.1.0 .build/release/ClaudeStatus-0.1.0.zip --notes "First release"
   ```

3. **Create your Homebrew tap** (the repo name **must** start with
   `homebrew-`).

   ```bash
   gh repo create homebrew-tap --public
   ```

4. **Copy the cask into the tap.** Replace `YOUR_GH_USERNAME` in
   `Casks/claude-status.rb` with your GitHub handle before committing.

   ```bash
   git clone https://github.com/<you>/homebrew-tap /tmp/homebrew-tap
   mkdir -p /tmp/homebrew-tap/Casks
   cp Casks/claude-status.rb /tmp/homebrew-tap/Casks/
   # edit URL/homepage placeholders in the copy, then:
   cd /tmp/homebrew-tap && git add -A && git commit -m "Add claude-status" && git push
   ```

5. **Users install via:**

   ```bash
   brew tap <you>/tap
   brew install --cask claude-status
   ```

To ship a new version: bump `version` in the cask, run `scripts/release.sh
<newver>`, update `sha256` from its output, cut a new GitHub release with
the new zip, push the cask change.

---

## Customizing

### Refresh interval

Edit `refreshInterval` in `UsageStore` (default 60s). Currently not
user-configurable from the UI.

### Pricing

Edit the `table` in `Pricing.swift`. Each entry is `(matchSubstring,
ModelPricing(input, output, cacheRead, cacheWrite))` in USD per million
tokens. The matcher uses a case-insensitive `contains` on the model id —
first match wins.

### Menu bar appearance

`App.swift` declares the label. Toggle "Show token count in menu bar" in
the dropdown to show/hide the number next to the ✨ icon.

### App icon

`scripts/make_icon.swift` draws the icon programmatically (orange gradient
+ white starburst). Edit and re-run, then rebuild.

---

## Future work

- **Notarization.** When you sign up for an Apple Developer ID, add a
  `notarize` step to `build.sh` (`codesign --options runtime` + `xcrun
  notarytool submit ... --wait` + `xcrun stapler staple`). No code changes
  needed.

- **Validate `/api/oauth/usage` against a fresh token.** After a fresh
  `claude /logout` + re-login, run the binary, click the icon, and check
  whether `apiUsage` populates. If it does and the response shape differs
  from what `UsageAPIClient.summarize()` understands, extend the
  `candidates` list with the new keys.

- **More plans in pricing.** If you start using older Sonnet/Haiku
  variants the price table still matches by substring. If Anthropic ships
  a new family (e.g. `claude-sonnet-5-x`), add a new entry.

- **Settings panel.** Refresh interval, show/hide cost column, choose
  which window appears in the menu bar title (today vs. week).

- **Diagnostics view.** A hidden "Show raw Keychain blob" / "Show raw
  /usage response" panel for debugging.
